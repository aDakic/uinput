#include <gtest/gtest.h>

#include "virtual_hmi/keyboard.hpp"

TEST(keyboard, constructor)
{
    // keyboard kb("keyboard", key_codes::space);
    // sleep(2);
    // while(1)
    // {
    //     sleep(1);
    //     kb.press(key_codes::space);
    //     kb.release(key_codes::space);
    // }
}
